# Game workspace

This directory is an isolated generation workspace.

- Keep every game artifact inside this directory.
- Use `index.html` as the browser entry point.
- Prefer a self-contained browser game with no required build step.
- Do not add benchmark scoring or evaluation code.
